apply from: "$rootDir/gradle/publishing.gradle"

description = 'Swagger2Markup Spring Rest Docs extension'

dependencies {
    compile project(":swagger2markup-extension-commons")
    testCompile project(":swagger2markup-test-commons")
}
